<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.cms.mapper.CmsArticleTagMapper">
    
    <resultMap type="CmsArticleTag" id="CmsArticleTagResult">
        <result property="tagId"    column="tag_id"    />
        <result property="articleId"    column="article_id"    />
        <result property="tagName"    column="tag_name"    />
    </resultMap>

    <sql id="selectCmsArticleTagVo">
        select tag_id, article_id, tag_name from cms_article_tag
    </sql>

    <select id="selectCmsArticleTagList" parameterType="CmsArticleTag" resultMap="CmsArticleTagResult">
        <include refid="selectCmsArticleTagVo"/>
        <where>  
            <if test="articleId != null "> and article_id = #{articleId}</if>
            <if test="tagName != null  and tagName != ''"> and tag_name like concat('%', #{tagName}, '%')</if>
        </where>
    </select>
    
    <select id="selectCmsArticleTagByTagId" parameterType="Long" resultMap="CmsArticleTagResult">
        <include refid="selectCmsArticleTagVo"/>
        where tag_id = #{tagId}
    </select>

    <insert id="insertCmsArticleTag" parameterType="CmsArticleTag">
        insert into cms_article_tag
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="tagId != null">tag_id,</if>
            <if test="articleId != null">article_id,</if>
            <if test="tagName != null and tagName != ''">tag_name,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="tagId != null">#{tagId},</if>
            <if test="articleId != null">#{articleId},</if>
            <if test="tagName != null and tagName != ''">#{tagName},</if>
         </trim>
    </insert>

    <update id="updateCmsArticleTag" parameterType="CmsArticleTag">
        update cms_article_tag
        <trim prefix="SET" suffixOverrides=",">
            <if test="articleId != null">article_id = #{articleId},</if>
            <if test="tagName != null and tagName != ''">tag_name = #{tagName},</if>
        </trim>
        where tag_id = #{tagId}
    </update>

    <delete id="deleteCmsArticleTagByTagId" parameterType="Long">
        delete from cms_article_tag where tag_id = #{tagId}
    </delete>

    <delete id="deleteCmsArticleTagByTagIds" parameterType="String">
        delete from cms_article_tag where tag_id in 
        <foreach item="tagId" collection="array" open="(" separator="," close=")">
            #{tagId}
        </foreach>
    </delete>
</mapper>